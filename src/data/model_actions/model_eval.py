import tensorflow as tf
import numpy as np
import pandas as pd

def evaluateModel(model, x_test, y_test) -> dict:
    # Evaluate the model
    # loss, acc = model.evaluate(x_test, y_test, verbose=0)
    return {}